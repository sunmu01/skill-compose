{
  "$schema": "./skills.schema.json",
  "skills": {
    "ragflow": {
      "required_env": [
        {
          "name": "RAGFLOW_API_KEY",
          "description": "RAGFlow API key (obtain from RAGFlow Web UI → Avatar → API Key)",
          "secret": true
        },
        {
          "name": "RAGFLOW_BASE_URL",
          "description": "RAGFlow server URL",
          "default": "http://host.docker.internal:8080"
        }
      ]
    }
  }
}
